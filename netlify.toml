[build]
  base = "apps/web"
  command = "pnpm install && pnpm build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "24"
  PNPM_VERSION = "8"

[[plugins]]
  package = "@netlify/plugin-nextjs"
